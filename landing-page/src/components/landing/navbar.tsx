import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { ArrowRight, Menu, X } from "lucide-react";
import { cn } from "@/lib/utils";
import { Container, Logo } from "./shared";
const NAV_LINKS=[{label:"Product",href:"#product",id:"product"},{label:"Solutions",href:"#solutions",id:"solutions"},{label:"Integrations",href:"#integrations",id:"integrations"},{label:"Pricing",href:"#pricing",id:"pricing"},{label:"Resources",href:"#resources",id:"resources"}] as const;
export function Navbar(){const[open,setOpen]=useState(false);const[active,setActive]=useState<string|null>(null);useEffect(()=>{const onKey=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};window.addEventListener("keydown",onKey);return()=>window.removeEventListener("keydown",onKey)},[]);useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;document.body.style.overflow="hidden";return()=>{document.body.style.overflow=previous}},[open]);useEffect(()=>{const sections=NAV_LINKS.map(l=>document.getElementById(l.id)).filter((el):el is HTMLElement=>Boolean(el));if(!sections.length)return;const io=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)setActive(visible.target.id)},{rootMargin:"-45% 0px -50% 0px",threshold:[0,.25,.5]});sections.forEach(s=>io.observe(s));return()=>io.disconnect()},[]);return <header className="sticky top-0 z-50 border-b border-border/70 bg-background/85 backdrop-blur-md"><Container><nav aria-label="Primary" className="flex h-16 items-center justify-between gap-4"><a href="#top" aria-label="NEXORA — back to top" className="rounded-sm"><Logo/></a><div className="hidden items-center gap-1 lg:flex">{NAV_LINKS.map(link=><a key={link.href} href={link.href} aria-current={active===link.id?"true":undefined} className={cn("rounded-md px-3 py-2 text-sm font-medium transition-colors hover:text-foreground",active===link.id?"text-foreground":"text-muted-foreground")}>{link.label}</a>)}</div><div className="hidden items-center gap-1 lg:flex"><Link to="/sign-in" className="btn-ghost">Sign in</Link><a href="#get-started" className="btn-primary">Get Started<ArrowRight className="size-4" aria-hidden="true"/></a></div><button type="button" className="inline-flex size-11 items-center justify-center rounded-md text-foreground transition-colors hover:bg-secondary lg:hidden" aria-expanded={open} aria-controls="mobile-nav" aria-label={open?"Close navigation menu":"Open navigation menu"} onClick={()=>setOpen(v=>!v)}>{open?<X className="size-5" aria-hidden="true"/>:<Menu className="size-5" aria-hidden="true"/>}</button></nav></Container>{open?<div id="mobile-nav" className="max-h-[calc(100dvh-4rem)] overflow-y-auto border-t border-border bg-background lg:hidden"><Container className="flex flex-col gap-1 py-4">{NAV_LINKS.map(link=><a key={link.href} href={link.href} onClick={()=>setOpen(false)} className="flex min-h-11 items-center rounded-md px-3 text-sm font-medium text-muted-foreground transition-colors hover:bg-secondary hover:text-foreground">{link.label}</a>)}<div className="mt-3 flex flex-col gap-2 border-t border-border pt-4"><Link to="/sign-in" onClick={()=>setOpen(false)} className="btn-outline min-h-11">Sign in</Link><a href="#get-started" onClick={()=>setOpen(false)} className="btn-primary min-h-11">Get Started<ArrowRight className="size-4" aria-hidden="true"/></a></div></Container></div>:null}</header>}
